name: event-crawl-schedule

on:
  schedule:
    # UTC 21:50 = KST 06:50 (월~금)
    - cron: '50 21 * * 0-4'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run:
    uses: ./.github/workflows/_crawl-jobs.yml
    secrets: inherit
